// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://hcalarcon.github.io",
  base: "/Portfolio/", // esto es importante
  outDir: "dist",
});
